OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_PROJECT=proj_XXXXXXXXXXXXX

# Vercel KV (required in production for persistent rate limiting)
KV_REST_API_URL=https://<region>.kv.vercel-storage.com
KV_REST_API_TOKEN=vercelKVTokenXXXXXXXXXXXXXXXXXXXXXXXX

# File uploads (Vercel Blob). Generate a Read/Write token in the Vercel dashboard and
# keep the real value in .env.local / deployment secrets (see README "Configuring Vercel Blob Tokens").
BLOB_READ_WRITE_TOKEN=

# Optional: set to "true" to disable attachments in local/dev without blob credentials
NEXT_PUBLIC_ATTACHMENTS_DISABLED=false